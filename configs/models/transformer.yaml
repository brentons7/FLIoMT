# Transformer — default hyperparameters for anomaly detection
# Source: tslib/scripts/anomaly_detection/MSL/Transformer.sh
# Tier 1 | Edge-compatible: yes

model:
  name: Transformer
  d_model: 64
  n_heads: 8
  e_layers: 1
  d_ff: 128
  dropout: 0.1
  # enc_in and c_out are set by the experiment config (depends on sensor channels)
