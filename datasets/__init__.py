"""
PyTorch Dataset classes and registry for physiological sensor data.

Modules:
    physio_dataset — PhysioDataset: sliding window dataset over .npy arrays
    registry       — dataset_registry: maps dataset keys to Dataset classes
"""
